# TESTE REAL — CLIENTES + INDEX/GERENCIADOR

1. Abra `index.html` e faça login no Supabase antigo.
2. Abra `painel-clientes.html`.
3. Edite o cliente/administrador usado para teste.
4. Marque as permissões desejadas e salve.
5. Abra `geradores/index.html` na mesma hospedagem.
6. Confira se aparecem somente as ações liberadas.
7. Teste os botões de abrir link e copiar código.
8. Não considere o módulo congelado até confirmar cada fluxo.

A conexão antiga foi mantida. O arquivo `dados/supabase-config.js` não foi substituído pelo projeto de desenvolvimento.
